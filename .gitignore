# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# Shopify CLI
.shopify/
*.zip

# Node (tooling only — the theme itself needs no build)
node_modules/
npm-debug.log*
yarn-error.log*
package-lock.json

# Secrets / local config — never commit
.env
.env.*
*.pem
.shopifyignore.local

# Temp
*.tmp
*.log
